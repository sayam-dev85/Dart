/*
What is bool?
  A bool variable can store only two values:
    true -> True
    false -> False
   Boolean is mainly use in conditions and decisions.
 */
void main(){
  bool isStudent = true;
  bool isTeacher = false;

  print(isStudent);
  print(isTeacher);
}